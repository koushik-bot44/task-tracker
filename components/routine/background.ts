/** The Well Being backdrop (2026-09-26): true = the animated "Waves" shader
    (components/routine/waves-background.tsx); false = the sky scene and the
    owner's picture as before. One line to switch, so a look can be tried and
    taken away again. */
export const WAVES_BACKGROUND = true;
