/* Superseded by `npx tsx --env-file=.env.local scripts/restructure-dryrun.ts --audit-only`.
 * The pre-migration audit this file produced (health/priority/gates/notes counts) is
 * obsolete now that the restructure has landed; it is kept only so old notes still resolve. */
